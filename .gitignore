node_modules
dist
dist-ssr
*.local
# Secrets / keys (never commit real credentials)
.env
.env.*
!.env.example
*.token
*.key
**/vercel_token
**/composio_key
gumroad-links.json
scripts/set-gh-secrets.py

coverage
.DS_Store
# Generated assets (built during CI) — but promo MUST be published by Pages
public/downloads/*
!public/downloads/README.md
# public/promo is generated then copied to dist by Vite — keep it tracked-ready

# dist is the build output — never ignore it (Pages artifact needs everything)
!dist/
!dist/**

.vercel
scripts/set-gh-secrets.py
